

services:
  neo4j:
    image: neo4j:latest
    container_name: neo4j-gitscribe
    ports:
      - "7474:7474" # HTTP Browser
      - "7687:7687" # Bolt protocol
    environment:
      - NEO4J_AUTH=neo4j/gitscribe_password
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
